# Macros
CXX=g++
CXXFLAGS= -O2 -g -fPIC -Wall -I..
LDFLAGS=-fPIC -shared
OBJ= RAL.o
LIB= libRAL.dll

# Reglas explicitas
all: libRAL.dll

new: clean all

clean:
	$(RM) $(OBJ) $(LIB)
	
libRAL.dll: RAL.o
	# Creo la libreria dinamica yaksRAL.dll
	$(CXX) $(LDFLAGS) -o $@ $^ -lws2_32
	rm RAL.o
